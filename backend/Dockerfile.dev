FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY tsconfig.json ./
COPY src/ ./src/
RUN chown -R node:node /app
USER node

CMD ["npx", "tsx", "watch", "src/index.ts"]

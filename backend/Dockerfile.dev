FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY tsconfig.json drizzle.config.ts ./
COPY src/ ./src/

CMD ["sh", "-c", "npx drizzle-kit push && npx tsx watch src/index.ts"]
